function onContinue(){
if user accept {
    
    POST /eros/v1/daters/${id}/profile_answers/
    {"question_id":"${consent_page_question_id}", "answer":"${answer}"}


post /eros/v1/daters/${id}/
{accepted_consent_page:true}

go to profile creation page 

} else {
    
    
    post /eros/v1/daters/${id}/
{accepted_consent_page:false, rejected:true}
}
}